import { DivisionValidatorDirective } from './division-validator.directive';

describe('DivisionValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DivisionValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
