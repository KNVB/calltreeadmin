import { LogRecipientValidatorDirective } from './log-recipient-validator.directive';

describe('LogRecipientValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LogRecipientValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
