import { SystemNameValidatorDirective } from './system-name-validator.directive';

describe('SystemNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SystemNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
